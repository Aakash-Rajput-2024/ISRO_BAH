"""Dataset generation utilities (the data/ folder is an importable package).

Generated datasets live in synthetic/ (regenerable) and real/ (provided);
both are git-ignored. See make_dataset.py (i.i.d. ensemble) and
make_sequence_dataset.py (temporal (N, T, D, D)).
"""
